{
  "name": "initial",
  "version": "1.0.0",
  "description": "this is a initial stage set json server for inital data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server-start" : "json-server --watch db.json"
   
  },
  "author": "Munira Akter",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
